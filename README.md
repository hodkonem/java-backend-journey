# java-backend-journey

Учебный **multi-module** проект для последовательного изучения Java backend-разработки:  
от базовых алгоритмов и структур данных до Hibernate, Spring, микросервисов и DevOps.

Проект развивается в формате, приближенном к реальному production-подходу:
- Git flow (feature-ветки + pull request)
- unit-тесты
- документация
- постепенное усложнение архитектуры

---

## 📦 Структура проекта

```
java-backend-journey
├── module-1-git-algorithms
│   └── Custom HashMap + unit tests
│
├── module-2-user-service
│   └── Console CRUD application (Hibernate + PostgreSQL)
│
├── docker-compose.yml
└── README.md
```

---

## 🔹 Module 1 — Git, алгоритмы и структуры данных

**Содержание:**
- собственная реализация `HashMap<K, V>`
- поддержка:
    - `put`, `get`, `remove`, `size`, `clear`
    - `null` key
    - resize с rehashing
- unit-тесты (JUnit 5)

**Цели модуля:**
- понять внутреннее устройство `HashMap`
- закрепить работу с Git (ветки, PR, merge)
- практика TDD / unit-тестирования

---

## 🔹 Module 2 — User Service (Hibernate + PostgreSQL)

**Описание:**  
Консольное Java-приложение без Spring, использующее Hibernate для работы с PostgreSQL.

**Функциональность (план):**
- CRUD-операции над сущностью `User`
- транзакционность
- DAO-паттерн
- логирование (SLF4J + Logback)
- обработка исключений

**Технологии:**
- Java 21
- Gradle
- Hibernate
- PostgreSQL
- Docker (для инфраструктуры)

---

## 🐘 Запуск PostgreSQL (Docker)

Для локальной разработки используется Docker Compose.

```bash
docker compose up -d
```

PostgreSQL будет доступен по адресу:

```
jdbc:postgresql://localhost:5432/user_service
```

---

## 🛠 Сборка проекта

Сборка всех модулей:

```bash
./gradlew clean build
```

Запуск тестов:

```bash
./gradlew test
```

---

## 📌 Примечания

- Проект учебный, но структура и подходы приближены к реальным корпоративным проектам
- Конфигурации и зависимости будут расширяться по мере прохождения модулей
- README будет дополняться по мере роста проекта

---

## 🇬🇧 English (short)

Educational multi-module Java backend project.

Includes:
- custom data structures (HashMap)
- console application with Hibernate + PostgreSQL
- Gradle multi-module setup
- Docker for local infrastructure

The project evolves step by step, following production-like practices.