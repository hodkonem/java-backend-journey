# java-backend-journey

Учебный **multi-module** проект для последовательного изучения Java backend-разработки:  
от базовых алгоритмов и структур данных до Hibernate, Spring, микросервисов и DevOps.

Проект развивается в формате, приближенном к реальному production-подходу:
- Git flow (feature-ветки + pull request)
- слоистая архитектура (DAO / Service)
- логирование и транзакционность
- постепенное усложнение архитектуры

---

## 📦 Структура проекта

```
java-backend-journey
├── module-1-git-algorithms
│   └── Custom HashMap + unit tests
│
├── module-2-user-service
│   ├── Console CRUD application
│   ├── Hibernate ORM
│   ├── PostgreSQL
│   ├── DAO + Service layers
│   └── Logging via Proxy pattern
│
├── docker-compose.yml
└── README.md
```

---

## 🔹 Module 1 — Git, алгоритмы и структуры данных

**Содержание:**
- собственная реализация `HashMap<K, V>`
- поддержка:
  - `put`, `get`, `remove`, `size`, `clear`
  - `null` key
  - resize с rehashing
- unit-тесты (JUnit 5)

**Цели модуля:**
- понять внутреннее устройство `HashMap`
- закрепить работу с Git (ветки, PR, merge)
- практика TDD / unit-тестирования

---

## 🔹 Module 2 — User Service (Hibernate + PostgreSQL)

**Описание:**  
Консольное Java-приложение без Spring, реализующее CRUD-операции над пользователями
с использованием Hibernate и PostgreSQL.

Приложение построено по слоистой архитектуре и имитирует backend-сервис
без web-контроллеров (CLI вместо REST).

---

### 🧩 Архитектура

```
Main (CLI)
  ↓
Service layer (UserService)
  ↓
DAO layer (UserDao)
  ↓
Hibernate / PostgreSQL
```

Дополнительно:
- логирование DAO-операций реализовано через **Proxy pattern**
- транзакции управляются на уровне Service
- DAO работает с уже открытой `Session`

---

### ⚙️ Функциональность

- CRUD-операции над сущностью `User`
- проверки входных данных
- контроль уникальности email
- транзакционность
- логирование времени выполнения DAO-методов
- маскирование чувствительных данных (email) в логах
- консольное меню управления пользователями

---

### 🧱 Сущность User

```
User {
  Long id
  String name
  String email
  Integer age
  LocalDateTime createdAt
}
```

- `id` — генерируется БД
- `createdAt` — инициализируется через `@PrePersist`
- `email` — логически уникален

---

### 🛠 Используемые технологии (Module 2)

- Java 21
- Gradle (multi-module)
- Hibernate ORM 7
- Jakarta Persistence
- PostgreSQL
- SLF4J + Logback
- Docker / Docker Compose
- `.env` конфигурация для локального запуска

---

## 🐘 Запуск PostgreSQL (Docker)

Для локальной разработки используется Docker Compose.

```bash
docker compose up -d
```

PostgreSQL будет доступен по адресу:

```
jdbc:postgresql://localhost:5432/user_service
```

Параметры подключения задаются через `.env` файл.

---

## ▶️ Запуск приложения (Module 2)

```bash
./gradlew :module-2-user-service:run
```

После запуска доступно консольное меню CRUD-операций.

---

## 🛠 Сборка проекта

Сборка всех модулей:

```bash
./gradlew clean build
```

Запуск тестов:

```bash
./gradlew test
```

---

## 📌 Примечания

- Проект учебный, но архитектура и подходы приближены к production
- Module 2 намеренно реализован **без Spring** для глубокого понимания Hibernate,
  транзакций и слоёв приложения
- В следующих модулях планируется:
  - unit-тестирование сервисов (JUnit + Mockito)
  - переход к Spring / Spring Boot
  - сравнение подходов «чистый Hibernate vs Spring Data JPA»

---

## 🇬🇧 English (short)

Educational multi-module Java backend project.

Includes:
- custom data structures (HashMap)
- console CRUD application with Hibernate + PostgreSQL
- layered architecture (DAO / Service)
- logging via Proxy pattern
- Gradle multi-module setup
- Docker for local infrastructure

The project evolves step by step, following production-like practices.

